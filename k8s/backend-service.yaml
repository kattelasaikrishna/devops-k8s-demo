apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: devops-demo
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080
  type: NodePort
